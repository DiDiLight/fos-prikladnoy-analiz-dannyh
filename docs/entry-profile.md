# Входной профиль дисциплины

## Назначение

Документ описывает знания и действия, которые нужны студенту до начала дисциплины «Прикладной анализ данных». Коды `ВК-1`–`ВК-4` введены внутри учебного комплекта, чтобы одинаково записывать пререквизиты, диагностику и результаты курса. Они не заменяют компетенции конкретной ОПОП.

Учебный контекст: направление 01.03.02 «Прикладная математика и информатика», 6-й семестр.

## Входные дисциплины и компетенции

| Входная компетенция | Предшествующие дисциплины | Индикатор | Что студент должен уметь |
|---|---|---|---|
| `ВК-1` — применяет базовый математический аппарат | математический анализ, линейная алгебра | `ВК-1.1` | интерпретировать производную, экстремум и градиент в простой задаче |
| `ВК-1` | математический анализ, линейная алгебра | `ВК-1.2` | выполнять операции с векторами и матрицами, учитывать расстояние и масштаб |
| `ВК-2` — применяет основы вероятности и статистики | теория вероятностей, математическая статистика | `ВК-2.1` | рассчитывать и объяснять описательные статистики, различать выборку и генеральную совокупность |
| `ВК-2` | теория вероятностей, математическая статистика | `ВК-2.2` | интерпретировать корреляцию, доверительный интервал и результат простой проверки гипотезы |
| `ВК-3` — использует Python для обработки данных | основы программирования | `ВК-3.1` | писать функции, работать с коллекциями, файлами и модулями |
| `ВК-3` | основы программирования | `ВК-3.2` | запускать ноутбук с начала и выполнять базовые операции в NumPy и pandas |
| `ВК-4` — выполняет базовую подготовку и выборку данных | технологии обработки данных, базы данных | `ВК-4.1` | различать типы и гранулярность данных, объединять, фильтровать и агрегировать таблицы |
| `ВК-4` | технологии обработки данных, базы данных | `ВК-4.2` | распознавать пропуски и дубликаты, составлять простой SQL-запрос |

## Проверка на входе

[КИМ-0](../Entry/kim-00-diagnostic.md) проводится на первой неделе и не входит в итоговые 100 баллов. Его результат нужен для адресного повторения: преподаватель отмечает недостигнутые индикаторы и назначает материалы из [карты выравнивания](../Entry/remediation-map.md).

Студент допускается к зависимому этапу после исправления тех пробелов, которые мешают выполнить соответствующее действие. Например, затруднение с объединением таблиц закрывается до построения паспорта данных, а ошибка в интерпретации корреляции — до выводов по EDA. Диагностика не заменяет КИМ-1–КИМ-6.

## Переход к результатам дисциплины

| Входная основа | Где используется | Целевой результат |
|---|---|---|
| `ВК-1`, `ВК-2` | EDA, статистические выводы, метрики | `BD-1.2`, `ML-2.3`, уровень С |
| `ВК-3`, `ВК-4` | воспроизводимая подготовка данных и признаков | `BD-1.3`, `BD-1.5`, `ML-2.2`, уровень С |
| все входные компетенции | постановка, моделирование и защита проекта | `LC-1.1`, `ML-2.1`, итоговый аналитический продукт |

Дальнейшее достижение рекомендованного для роли Data Analyst уровня П по `BD-1` требует нескольких самостоятельных работ в новых условиях. Возможные элементы, артефакты и формы контроля приведены в [траектории роли](role-trajectory.md).
